#ifndef RLE_HPP
#define RLE_HPP

#include <string>
#include <vector>
#include <bits/stdc++.h>

std::string run_length_encode(const std::string&);
std::string run_length_decode(const std::string&);

#endif // RLE_HPP
